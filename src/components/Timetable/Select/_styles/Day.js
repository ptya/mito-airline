import styled from "styled-components";

import { colors } from "components/_styles/variables";

const Day = styled.h3`
  font-size: 18px;
  color: ${colors.grey5};
  font-weight: normal;
`;

export default Day;
